import os

BASE_DIR = '../'
PLOTS = os.path.join(BASE_DIR, 'screenshots/')

format = 'webp'

INTERACTIVE = False  # False, Display on plots as webp and render to display, works on GitHub. Set to True to use dynamic plots in supported environments
